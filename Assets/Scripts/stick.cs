using System.Collections; using System.Collections.Generic; using UnityEngine; using UnityEngine.UI; using UnityEngine.EventSystems;  public class stick : MonoBehaviour // IDragHandler, IPointerUpHandler, IPointerDownHandler {     private Image bgImg;     private Image joystickImg;     private Vector3 InputVector;      protected Joystick joystick;      //protected     // Use this for initialization      void Start()     {          joystick = FindObjectOfType<Joystick>();      } // Update is called once per frame      void Update()     {      Vector3 pos = GetComponent<Transform>().position;      GetComponent<Transform>().position = new Vector3(joystick.Horizontal * 2f, -3.3f, 0);     }  }  